# 使用临时 Pine 脚本在 TradingView 验证边界

最终唯一可运行交付物是生产版 `chanlun.pine`。开发期间可生成不交付的临时 Pine v6 测试脚本，用合成数组覆盖结构边界；生产指标还需在真实品种、多周期和最重配置下验证编译、运行、重绘及资源预算。所有测试均通过 TradingView Pine Editor 完成，每个用例结束后立即从图表移除测试指标；最终指标只保留轻量不变量诊断，验证日期、配置、结果和截图写入测试报告。
