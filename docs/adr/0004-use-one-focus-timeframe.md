# 使用单一焦点周期承载完整绘图

指标以一个可切换的焦点周期展示最大可计算范围内的完整缠论结构，并让当前周期与最多四个更高参考周期只提供已收盘结构状态、买卖点及共振信息。更低周期的完整结构通过切换图表周期查看。生产指标不为未收盘参考 K 复制完整递归引擎；参考预览即使存在也只能是可省略的隔离显示能力。这个设计用单个 Pine 指标适配 TradingView 免费版，同时避免跨周期请求、执行时间、内存和绘图对象上限导致结果残缺。
