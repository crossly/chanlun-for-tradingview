# 只交付官方 Pine v6 指标并在 TradingView 验证

项目只交付遵循 TradingView 官方 Pine v6 语法与 API 的单文件指标，不再建设或集成 PineTS、本地解析器、兼容编译器或其他语言参考引擎，也不因社区工具缺失而限制官方语法。所有编译与运行测试都通过 TradingView Pine Editor 的 Save 和 Add to chart 完成；每个测试结束后立即从图表移除测试指标，以释放免费计划最多两个指标槽位。TradingView 官方结果是唯一编译与运行判据。
