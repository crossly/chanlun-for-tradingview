# 重接事件、界面与参考周期

Type: task
Status: ready-for-agent
Blocked by: 03

## Goal

把新核心接入不可变事件账本、参考周期、共振、区间套、情景和现有视图外壳。

## Acceptance

- 使用 `B/S/Tn/TF` 标签和 course-v2 事件 ID。
- 历史状态迁移不可变，活动状态允许重建。
- 参考周期预览不进入确认共振和默认提醒。
- 所有显示开关只影响呈现。

## Comments

- 2026-07-16：焦点与参考周期在历史确认柱推进时增量重建并 reconcile；重载可重放候选、确认、替换、失效及走势阶段原因事件，事件 ID 固定包含 `course-v2`。官方编译和运行通过，完整事件 ID 重载对比仍待 ticket 05。
- 2026-07-16：生命周期来源改为稳定结构 ID；事件身份加入 `knownTime + occurrence`，同一结构阶段往返不会被去重吞掉。极端结构次序重排下 reconcile 仍有平方级预算风险，保留到具备官方性能回归证据后再评估 `map` 优化。
- 2026-07-16：后续实现取代上一条性能与身份方案：reconcile 已使用 `map<string, int>` 索引 `slotId`，焦点与四个参考引擎各自只在 dirty 时重放；事件序号直接取账本长度，事件 ID 使用 `structureId + reason + sequence`。实时预览不写确认账本，生命周期来源分别绑定背驰/一类点/中枢的稳定结构 ID。
- 2026-07-16：为修复重载内存上限，`LifecycleEvent` 从 10 字段压缩为 7 个必要字段并由 `var` 持有；`SKHYNIXUSDT.P / 5m / 4 自动参考 / 2,000 K` 首次运行和页面重载均恢复约 `4463` 个历史事件，无超时、运行或内存错误。测试实例已移除。
- 2026-07-17：新笔恢复正常吞吐量后，四参考周期再次暴露内存上限。隔离测试确认 `0-3` 个参考周期正常、第 `4` 个触发错误；根因是每个参考请求返回确认与活动柱共 10 条序列，并复制一整套仅供面板计数、从未参与快照/证据/共振的预览引擎。生产实现改为每周期 5 条已确认序列并删除参考预览副本，四参考周期首次运行及重载均无错误。
